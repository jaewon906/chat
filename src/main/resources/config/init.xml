<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.test.chat.mapper.signup.SignupMapper">
    <insert id="createTbComUser">
        create table TB_COM_USER
        (
            user_id varchar(100) not null,
            pwd varchar(100) not null,
            email varchar(100),
            create_date timestamp ,
            update_date timestamp,
            primary key(user_id)
        )
    </insert>
    <insert id="createTbComRole">
        create table TB_COM_ROLE
        (
            role_cd varchar(100) not null,
            role_nm varchar(100) not null,
            primary key(role_cd)
        )
    </insert>
    <insert id="createTbComRoleUser">
        create table TB_COM_ROLE_USER
        (
            role_cd varchar(100) not null,
            user_id varchar(100) not null,
            primary key(role_cd, user_id)
        );
    </insert>
</mapper>